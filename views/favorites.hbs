<!DOCTYPE html>
<html lang="en">
<head>
    {{> header}}
</head>
<body>
    <div>
        {{> navbar}}
        <div class="lg:flex-wrap lg:flex mx-auto w-4/5 mt-16 md:flex md:flex-wrap md:content-between md:justify-center">
            {{#if rows}}
                {{#each rows}}
                <div class="wish-list mt-10 lg:mx-10 mb-20 pb-10 lg:pb-10 bg-white w-full lg:w-96 md:w-72 md:mr-6 md:-mt-5 relative rounded-l-3xl rounded-r-3xl border-b-4 border-t-4 border-blue-500 shadow-2xl" data-aos="fade-up" data-aos-duration="1100">
                    <div class="favorite-imgs w-36 h-36 mx-auto pt-8 mt-5 mb-5 rounded-full" style="background: url(uploads/users/{{ img }}) center; background-size: cover;" data-aos="zoom-in" data-aos-duration="1000"></div>
                    <!-- <img src="/assets/img/ornaments.png" class="absolute w-20 -top-5 -left-4"> -->
                    <h1 class="text-center text-3xl ml-5 mr-5 lg:ml-7 lg:mr-7 font-bold mb-7">{{ name }}</h1>
                    <button class="flex mx-auto bg-blue-400 text-white font-bold text-xl px-5 py-2 rounded-l-2xl rounded-r-2xl border-b-4 border-blue-600 hover:bg-blue-600 transition duration 500" data-aos="zoom-out" data-aos-duration="1000"><a href="/users/{{ username }}">Ver perfil</a></button>
                    <h1 class=" text-center absolute -bottom-1 w-full font-bold bg-blue-600 text-xl rounded-bl-3xl rounded-br-3xl py-2 text-white"></h1>
                </div>
                {{/each}}
            {{else}}
                <h1 class="font-bold text-3xl">No hay favoritos, agrega algunos usuario a tu lista de favoritos!</h1>
            {{/if}}
        </div>
    </div>

    {{> footer}}
</body>
</html>